# Specs: long-screenshot

## 新增需求：编辑方式列表项统一高度

### 场景

编辑方式中间页已经具备列表结构和项间距，但当前不同入口项高度不一致。系统应统一每个入口项的高度，提升列表整齐度。

### 要求

- 编辑方式中间页必须继续使用列表展示入口。
- 每个入口项必须保持统一高度。
- 点击列表项后，系统必须继续进入对应编辑页。

### 验收

- 当前所有入口项显示为统一高度。
- 点击行为与现有导航保持一致。