# 变更规格：拒绝评分邀请后不再重复弹窗

## 变更点

### 保存成功后的评分邀请

- 保存长截图成功后，应用仍可在成功提示关闭后展示评分邀请。
- 评分邀请只允许在用户尚未完成评价、且尚未明确拒绝提示时出现。
- 评分邀请必须包含两个动作：
  - `不再提示`：记录用户拒绝决定，后续保存成功不再弹出评分邀请。
  - `去给好评`：跳转到 App Store 评价页，并记录该邀请已处理，后续保存成功不再弹出评分邀请。

## 验收

- 用户点击 `不再提示` 后，本地状态可跨应用重启保留。
- 用户点击 `去给好评` 后，仍然满足“不再重复弹窗”。
