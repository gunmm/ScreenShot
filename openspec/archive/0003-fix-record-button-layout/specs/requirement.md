# Spec: 开始录制按钮多语言适配布局

## 用户故事（User Story）

- 作为一名海外用户，我在使用英文版的应用时，我希望“Start Recording”的按钮能够正确显示，且不会超出底部的录制容器边界，以保证良好的界面观感。

## 边界条件（Edge Cases）

- 中文环境下的短文本：宽度应该保持在 160pt 以免显得过于紧缩，导致点击区域小。
- 英文环境下的长文本：宽度自动撑开，包住文字。

## 验收标准

- 文本不会溢出背景框，且文字右侧应该与背景框有合适的间距（例如 16pt）。
- `recordContainer` 的最小宽度为 160pt。
