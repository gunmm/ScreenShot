# Specs: long-screenshot

## 新增需求：编辑方式列表项增加分组间距

### 场景

编辑方式中间页已经改成列表，但当前各入口项仍连在一起显示。系统应让每个入口项以分开的分组形式展示，提升识别度。

### 要求

- 编辑方式中间页必须继续使用列表展示入口。
- 每个入口项之间必须有明确的视觉间距，不再连成同一块。
- 点击列表项后，系统必须继续进入对应编辑页。

### 验收

- 列表项之间存在明确的垂直分组间距。
- 点击行为与现有导航保持一致。