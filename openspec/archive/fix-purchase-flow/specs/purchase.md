# Purchase Flow Specifications

## 面向用户
- 当用户试用期过去后，点击保存时系统弹窗提示解锁。
- 用户点击“去解锁”按钮，App 显示支付请求提示，并随即调起苹果 App Store 内购模块。
- 用户完成支付后，应用应当自动执行保存长截图操作。

## 异常边界
- 极少数情况下（如连续快速点击或动画未完成时），原先因为在 `UIAlertController` 消失动画未完成时调用 `present` 会导致系统吞掉或延迟新的弹窗，从而直接导致支付逻辑失败。
- 新的实现必须保证支付相关调用不依赖于 UI 弹窗动画完成回调。
