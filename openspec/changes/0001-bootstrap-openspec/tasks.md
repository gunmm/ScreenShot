# Tasks: 落地 OpenSpec 工作流

## 实施清单

- [x] 新增根目录 `AGENTS.md`（OpenSpec-first 约束）
- [x] 新增 `openspec/README.md`
- [x] 新增主规格目录 `openspec/specs/` 与索引
- [x] 迁移正文到 `openspec/specs/long-screenshot.md`
- [x] 新增 `openspec/changes/_template/`（proposal/specs/design/tasks）
- [x] 新增 `openspec/archive/` 与 `openspec/schemas/` 占位说明
- [x] 将根目录 `OPENSPEC.md` 改为入口索引（去重）

## 自测步骤（手工）

- [ ] 打开 `openspec/specs/long-screenshot.md`，确认能覆盖端到端流程与验收标准
- [ ] 复制 `openspec/changes/_template/` 新建一个变更目录，确认结构齐全

