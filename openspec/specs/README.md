# 已实现系统规格（Source of Truth）

本目录下的文档是“已实现系统”的**真相来源**：

- 任何代码行为的改变，都必须同步更新这里
- 每个变更应先在 `openspec/changes/<change>/` 写提案与规格，再把最终结论沉淀到这里

## 规格索引

- `long-screenshot.md`：LongScreenShot 端到端规格（采集、分片、拼接、保存）

